'use strict';$('#mediaUploadForm').addEventListener('submit',async(event)=>{event.preventDefault();const button=event.currentTarget.querySelector('button');const status=$('#mediaStatus');button.disabled=true;button.textContent='UPLOADING…';try{const form=new FormData();form.append('file',$('#mediaFile').files[0]);form.append('alt',$('#mediaAlt').value);const result=await request('/.netlify/functions/admin-upload-media',{method:'POST',body:form});catalogData.media.unshift(result.media);renderMedia();event.currentTarget.reset();setMessage(status,'Image uploaded to the media library.',true);}catch(error){setMessage(status,error.message);}
finally{button.disabled=false;button.textContent='UPLOAD IMAGE';}});function renderOperations(){const active=operationsData.codes.filter((code)=>code.status==='active');const pending=operationsData.redemptions.filter((redemption)=>redemption.status==='pending_fulfillment');$('#orderCount').textContent=operationsData.orders.length;$('#activeCount').textContent=active.length;$('#redemptionCount').textContent=pending.length;const eligible=catalogData.catalog.products.filter((product)=>product.giveOneEligible&&product.status!=='archived');$('#giveProductId').innerHTML=eligible.map((product)=>`<option value="${escapeHtml(product.id)}">${escapeHtml(product.shortName)} — ${escapeHtml(product.audienceLabel)}</option>`).join('');$('#redemptions').innerHTML=operationsData.redemptions.map((redemption)=>`<tr class="border-b border-white/5"><td class="p-4 font-mono">${escapeHtml(redemption.confirmation)}</td><td class="p-4">${escapeHtml(redemption.recipient.firstName)} ${escapeHtml(redemption.recipient.lastName)}<br><span class="text-slate-400">${escapeHtml(redemption.recipient.email)}</span></td><td class="p-4">${escapeHtml(redemption.productName)}<br>${escapeHtml(redemption.fit || '—')} · Size ${escapeHtml(redemption.size)}${redemption.color ? `· ${escapeHtml(redemption.color)}` : ''}</td><td class="p-4">${escapeHtml(redemption.recipient.address1)}${redemption.recipient.address2 ? `<br>${escapeHtml(redemption.recipient.address2)}` : ''}<br>${escapeHtml(redemption.recipient.city)}, ${escapeHtml(redemption.recipient.state)} ${escapeHtml(redemption.recipient.postalCode)}</td><td class="p-4">${escapeHtml(redemption.status)}${redemption.tracking ? `<br><span class="text-slate-400">${escapeHtml(redemption.tracking)}</span>` : ''}</td><td class="p-4">${new Date(redemption.createdAt).toLocaleString()}</td><td class="p-4">${redemption.status !== 'fulfilled' ? `<button class="text-amber-400 font-bold"data-fulfill="${escapeHtml(redemption.confirmation)}">MARK FULFILLED</button>` : '—'}</td></tr>`).join('');$$('[data-fulfill]').forEach((button)=>button.addEventListener('click',async()=>{const tracking=prompt('Tracking number or fulfillment note (optional):')||'';try{await request('/.netlify/functions/admin-update-redemption',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({confirmation:button.dataset.fulfill,status:'fulfilled',tracking})});await loadAll();}catch(error){alert(error.message);}}));}
$('#createCodes').addEventListener('submit',async(event)=>{event.preventDefault();try{const result=await request('/.netlify/functions/admin-create-codes',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({productId:$('#giveProductId').value,orderRef:$('#orderRef').value,count:Number($('#count').value)})});alert(result.created.map((entry)=>entry.code).join('\n'));await loadAll();}catch(error){alert(error.message);}});$('#refresh').addEventListener('click',loadAll);$('#export').addEventListener('click',()=>{const rows=[['Confirmation','Code','Product','Variant ID','Fit','Size','Color','First Name','Last Name','Email','Address 1','Address 2','City','State','ZIP','Status','Created'],...operationsData.redemptions.map((redemption)=>[redemption.confirmation,redemption.code,redemption.productName,redemption.variantId,redemption.fit,redemption.size,redemption.color,redemption.recipient.firstName,redemption.recipient.lastName,redemption.recipient.email,redemption.recipient.address1,redemption.recipient.address2,redemption.recipient.city,redemption.recipient.state,redemption.recipient.postalCode,redemption.status,redemption.createdAt])];const csv=rows.map((row)=>row.map((value)=>`"${String(value ?? '').replaceAll('"', '""')}"`).join(',')).join('\n');const anchor=document.createElement('a');anchor.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));anchor.download='izhe-redemptions.csv';anchor.click();URL.revokeObjectURL(anchor.href);});
